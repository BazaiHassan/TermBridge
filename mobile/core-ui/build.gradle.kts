// Design system: theme, typography (JetBrains Mono, OFL-1.1) and shared components.
plugins {
    alias(libs.plugins.termbridge.android.library)
    alias(libs.plugins.termbridge.android.compose)
}

android {
    namespace = "io.termbridge.core.ui"
}
